# Phase 3 Synthetic Fixture Rules

- This fixture is isolated from production code and credentials.
- Use Python standard-library `unittest`; do not add dependencies or lockfiles.
- Keep public functions typed and deterministic.
- Modify only paths explicitly assigned by the Worker brief.
- Do not commit, push, deploy, publish, or access remote systems.
